/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const gruposAPI_auto = {

    /**
    * Gets all grupos
    */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/grupos`,requestOptions);
        return response.data;
    },

    /**
    * Gets an entry from 'grupos' by its primary key
    */
    getById: async function(grupoId) {
        let response = await axios.get(`${BASE_URL}/grupos/${grupoId}`,requestOptions);
        return response.data[0];
    },

    /**
    * Creates a new entry in 'grupos'
    */
    create: async function(formData, ) {
        let response = await axios.post(`${BASE_URL}/grupos`,formData, requestOptions);
        return response.data;
    },

    /**
    * Updates an existing entry in 'grupos' by its primary key
    */
    update: async function(formData, grupoId) {
        let response = await axios.put(`${BASE_URL}/grupos/${grupoId}`,formData, requestOptions);
        return response.data;
    },

    /**
    * Deletes an existing entry in 'grupos' by its primary key
    */
    delete: async function(grupoId) {
        let response = await axios.delete(`${BASE_URL}/grupos/${grupoId}`,requestOptions);
        return response.data;
    },
};

export {gruposAPI_auto};