/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const comentariosAPI_auto = {

    /**
    * Gets all comentarios
    */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/comentarios`,requestOptions);
        return response.data;
    },

    /**
    * Gets an entry from 'comentarios' by its primary key
    */
    getById: async function(comentarioId) {
        let response = await axios.get(`${BASE_URL}/comentarios/${comentarioId}`,requestOptions);
        return response.data[0];
    },

    /**
    * Creates a new entry in 'comentarios'
    */
    create: async function(formData, ) {
        let response = await axios.post(`${BASE_URL}/comentarios`,formData, requestOptions);
        return response.data;
    },

    /**
    * Updates an existing entry in 'comentarios' by its primary key
    */
    update: async function(formData, comentarioId) {
        let response = await axios.put(`${BASE_URL}/comentarios/${comentarioId}`,formData, requestOptions);
        return response.data;
    },

    /**
    * Deletes an existing entry in 'comentarios' by its primary key
    */
    delete: async function(comentarioId) {
        let response = await axios.delete(`${BASE_URL}/comentarios/${comentarioId}`,requestOptions);
        return response.data;
    },
};

export {comentariosAPI_auto};